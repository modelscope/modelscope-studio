import type { Components } from 'react-markdown';

// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface FlushingEndProps {}

export const FlushingEnd: Components['flushing-end'] = () => {
  return null;
};
